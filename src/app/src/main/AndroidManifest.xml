<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.staffmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NonAdmin.UserProfileActivity.UserProfileActivity"></activity>
        <activity android:name=".NonAdmin.RequestCrudActivity.RequestCrudActivity" />
        <activity android:name=".Admin.UserManagementActivity.AdminInformationActivity" />
        <activity android:name=".Admin.UserRequestActivity.UserRequestActivity" />
        <activity android:name=".NonAdmin.RequestActivity.RequestActivity" />
        <activity android:name=".Admin.MainAdminActivity.MainAdminActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>